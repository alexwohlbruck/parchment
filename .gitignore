.DS_Store
valhalla
node_modules
dist
.env
.env.test
# E2E env with secrets – do not commit
web/e2e/env.test
# Infisical CLI project link (local dev convenience; keeps repo provider-agnostic)
.infisical.json

# Playwright
playwright-report/
web/playwright-report/
web/test-results/

# Maizzle email build output
server/emails/output/

# Playwright saved sign-in state (contains a live session cookie)
web/e2e/.auth/

# Per-branch preview worktrees (scripts/preview.sh)
.claude/worktrees/

# Preview tooling writes this per worktree
.env.preview
